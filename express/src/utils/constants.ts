/**
 * @module Constants
 * @description This module provides constant values, including paths and configuration values.
 */

export const PORT: number = 3000;

export const RELATIVE_PATHS = {
    USERS_DB: '../../database/users.db',
};
